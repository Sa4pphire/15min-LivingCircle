name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            backend/requirements.txt
            backend/requirements-dev.txt

      - name: Test Python API
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements-dev.txt
          pytest backend/tests

      - name: Build and test C++ engine
        run: |
          cmake -S cpp-engine -B cpp-engine/build -DCMAKE_BUILD_TYPE=Release
          cmake --build cpp-engine/build --parallel
          ctest --test-dir cpp-engine/build --output-on-failure

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Build frontend
        working-directory: frontend
        run: |
          npm ci
          npm run build

      - name: Build container
        run: docker build -t fifteen-minute-life-circle:ci .
